import React from 'react';

import Nav from '../../organism/Nav'
import {List} from '../../organism/List'

export const Home = () => {
    return (
        <section>
          <Nav/>
          <List/>
        </section>
    )
}